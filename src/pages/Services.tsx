import PageHero from "../components/ui/PageHero";
import Icon, { type IconName } from "../components/ui/Icon";
import FinalCTA from "../components/site/FinalCTA";
import { services } from "../data/siteData";
export default function Services() { return <><PageHero eyebrow="SERVICES" title="Technology That Removes Bottlenecks." copy="We design and build the connected systems behind faster operations, better customer experiences, and scalable growth."/><section className="section page-section"><div className="container service-detail-list">{services.map((service, index) => <article className="service-detail" key={service.title}><div className="service-index">0{index + 1}</div><div className="card-icon"><Icon name={service.icon as IconName}/></div><div className="service-detail-main"><h2>{service.title}</h2><p className="lead">{service.short}</p><p>{service.description}</p><h3>The problem it solves</h3><p>{service.problem}</p></div><div className="service-detail-side"><h3>What’s included</h3><ul>{service.included.map(item => <li key={item}><Icon name="check"/>{item}</li>)}</ul><h3>Example use cases</h3><div className="tags">{service.useCases.map(item => <span key={item}>{item}</span>)}</div><small>Suggested stack</small><b>{service.stack}</b></div></article>)}</div></section><FinalCTA /></>; }
